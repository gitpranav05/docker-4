name: Deploy the backend

on:
  push:
    braanches: [main]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name : Checkout code
        uses: actions/checkout@v2

      - name: Docker login
        uses: docker/login-action@v2
        with :
          usename: ${{secrets.DOCKERHUB_USERNAME}}
          passworrd: ${{secrets.DOCKERHUB_TOKEN}}